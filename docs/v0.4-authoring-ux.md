# v0.4 Authoring UX (guide hub)

**Goal:** writing RedByteFX feels teachable and debuggable for authors coming from raw AGSL or Shadertoy-style shaders.

This hub links the **first complete v0.4 documentation pass** in-tree. It is still not a publication contract; the library remains pre-publication.

## Read this first

| Document | Purpose |
|----------|---------|
| [agsl-vs-redbytefx.md](agsl-vs-redbytefx.md) | Mental model: what the DSL guarantees, what stays visible in AGSL, uniform handles, coordinates, sampling space. |
| [cookbook-patterns.md](cookbook-patterns.md) | Translation tables, bad→good fixes, and end-to-end rewrites (wave, masks, compositing, radar-style polar). |
| [runtime-authoring-checklist.md](runtime-authoring-checklist.md) | `compile → inspect → bind → apply`, same-effect `FxParam` rule, controller-per-target, sampling checks. |
| [library-limitations.md](library-limitations.md) | One index of platform/runtime limits and supported patterns (links to deeper docs). |
| API (`:redbytefx-core`) | Structured failures: **`FxDiagnosticException`**, **`FxDiagnostic`**, **`FxDiagnosticCode`** (`FxDiagnostics.kt`) for compile/bind errors (exception message remains human-readable). |

Supporting material:

- [backlog-v0.4-v0.6.md](backlog-v0.4-v0.6.md) — full backlog; v0.4 section notes what shipped vs nice-to-have.
- [roadmap.md](roadmap.md) — where v0.4 sits relative to tooling (`v0.3`) and runtime (`v0.5`).

## What this milestone added (author-facing)

- **Compiler/runtime messages:** clearer text when a uniform `FxParam` does not belong to the bound effect; hints when raw Kotlin values (ints, strings, booleans) reach the shader emitter instead of DSL expressions.
- **`fn` return-type validation** (see [CHANGELOG.md](../CHANGELOG.md)): body type must match declared `FxValueType` before codegen.
- **Docs:** expanded AGSL comparison, compact **porting checklist**, cookbook promoted from “draft” to maintained companion, cross-links from README.
- **KDoc:** `sample(...)` vs stdlib `sampleUv(...)` called out in core/stdlib/Compose where authors look first.

## Explicitly out of scope for this pass

- IDE-style diagnostic **pipeline** (streaming to editors); structured **`FxDiagnostic`** on **`FxDiagnosticException`** is the supported programmatic surface for now.
- Source maps or line-level shader↔Kotlin mapping.
- IDE features inside `:sample` (it stays a demo, not an IDE).

Future incremental work can deepen the cookbook and tighten naming across `stdlib` under **v0.6 Library shaping**, without reopening v0.4 as a blank slate.
